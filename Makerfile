OUT = Thunder-Keys
FILE = TypingSpeedTest.go
all: linux windows mac

linux:
    GOOS=linux GOARCH=amd64 go build -o $(OUT)-linux $(FILE)

windows:
    GOOS=windows GOARCH=amd64 go build -o $(OUT)-win.exe $(FILE)

mac:
    GOOD=darwin GOARCH=arm64 go build -o $(OUT)-mac $(FILE)


